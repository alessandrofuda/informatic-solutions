# FROM cytopia/nginx-mainline
#
# RUN sed -i 's!/var/www/html!/var/www/html/public!g' /etc/nginx/conf.d/localhost.conf
FROM nginx:1.10

ADD ./vhost.conf /etc/nginx/conf.d/default.conf
WORKDIR /var/www/default/htdocs
